# n8n workflows
Always use n8n-mcp tools (search_nodes, get_node_types) before configuring any node.
Expression syntax: {{$json.fieldName}} always — never $json['field'].
Validate before saving. Test in n8n UI before marking complete.
